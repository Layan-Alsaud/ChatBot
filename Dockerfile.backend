FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

COPY backend.py ./

RUN mkdir -p /app/chat_logs /app/pdf_store

EXPOSE 5000

CMD ["uvicorn", "backend:app", "--host", "0.0.0.0", "--port", "5000"]
